# The Get-WinEvent -ListLog * command in PowerShell gives a detailed list of all event logs available on your computer, including both traditional logs and newer logs that are more specific to modern Windows features.
Get-WinEvent -ListLog *
